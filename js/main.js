import {showFilePreview, removeFilePreview} from './methods.js'
window.showFilePreview = showFilePreview
window.removeFilePreview = removeFilePreview

const ready = () => {

}

document.addEventListener('DOMContentLoaded', ready)

